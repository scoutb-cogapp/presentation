+++
title = "Image and Text"
weight = 50
template = "slide-image-text.html"

[extra]
image = "images/placeholder.svg"
image_alt = "Describe the image here for accessibility"
image_first = false

intro = "An optional line that sits below the heading — useful for a subtitle or context. It will grow to contain your content."
notes = "Point to the image when making this argument. The key stat is in the caption."
+++

This is the text that sits alongside the image. Write it as normal Markdown.

- You can use bullets here
- Or plain paragraphs
- Or mix in **bold** and *italic*

Set `image_first` to `true` or `false` in the frontmatter. On wide screens this puts the image on the left or right of the text; on narrow screens it puts the image above or below the text.
